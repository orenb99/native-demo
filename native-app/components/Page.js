import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Page = ({ type }) => {
  return (
    <View>
      <Text>Page</Text>
    </View>
  );
};

export default Page;

const styles = StyleSheet.create({});
